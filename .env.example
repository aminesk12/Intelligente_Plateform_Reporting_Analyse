# Copy to `.env` and fill in. Loaded automatically by the config loader.
# NEVER commit the real .env — it holds secrets.

# --- Logging ---
ETL_LOG_LEVEL=INFO

# --- Database ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=analytics
DB_USER=etl_user
DB_PASSWORD=change-me

# --- REST API ---
API_TOKEN=
# API_KEY=
# API_USER=
# API_PASSWORD=

# --- SAP OData ---
SAP_BASE_URL=https://sap-host:443/sap/opu/odata/sap/ZSALES_SRV
SAP_CLIENT=100
SAP_USER=
SAP_PASSWORD=
