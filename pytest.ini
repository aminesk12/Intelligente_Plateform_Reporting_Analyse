[pytest]
# Make the project root importable so `from ETLS...` works when running
# pytest from anywhere in the repo.
pythonpath = .
testpaths = tests
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
